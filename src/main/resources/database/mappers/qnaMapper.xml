<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
		    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
		    
<mapper namespace="com.iu.start.board.qna.QnaDAO">
	<select id="getList" resultType="QnaDTO" parameterType="map">
		SELECT num, title, writer, regdate, hit, ref, step, depth 
		FROM (select rownum rown, Q.* from QNA Q ORDER BY NUM DESC)
		WHERE rownum ${startRow} and ${lastRow} 
	</select>	
	<select id="getDetail" resultType="QnaDTO" parameterType="BoardDTO">
		SELECT * FROM QNA WHERE NUM=#{num}
	</select>
	<insert id="setAdd" parameterType="BoardDTO">
		INSERT INTO QNA (NUM, TITLE, CONTENTS, WRITER, REGDATE, HIT, REF, STEP, DEPTH)
		VALUES (BOARD_SEQ.NEXTVAL, #{title}, #{contents}, #{writer}, SYSDATE, 0, 0, 0, 0)
	</insert>
	<update id="setUpdate" parameterType="BoardDTO">
		UPDATE QNA SET TITLE=#{title} , CONTENTS=#{contents} WHERE NUM=#{num}
	</update>
	<delete id="setDelete" parameterType="BoardDTO">
		DELETE QNA WHERE NUM=#{num}
	</delete>
	

</mapper>